const { arraySum } = require('./arrays');
const { greaterThanOrEqual } = require('./comparators');
const { positive, nonZero } = require('./numbers');

module.exports = {
  arraySum,
  greaterThanOrEqual,
  positive,
  nonZero
};
