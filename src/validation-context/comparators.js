module.exports = {
  greaterThan: (value, target) => value > target,
  greaterThanOrEqual: (value, target) => value >= target,
  equal: (value, target) => value === target,
  lessThan: (value, target) => value < target,
  lessThanOrEqual: (value, target) => value <= target
}
