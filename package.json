{
  "name": "data-transform-descriptor",
  "version": "0.0.3",
  "description": "A tiny module to describe and implement data transforms/mappings.",
  "main": "dist/index.js",
  "engines": {
    "node": ">=7.0.0"
  },
  "scripts": {
    "test": "mocha --recursive",
    "test:watch": "mocha --recursive --watch",
    "build": "./node_modules/.bin/babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "data-structure-validator": "0.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
